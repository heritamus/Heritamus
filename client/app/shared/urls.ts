let baseUrl = 'https://github.com/heritamus/Heritamus';

export class Urls {
  static project = `${baseUrl}`;
  static license = `${baseUrl}/blob/master/LICENSE`;
  static doc = `${baseUrl}/wiki`;
  static helpCenter = `${baseUrl}/wiki`;
  static team = `${baseUrl}/wiki/Team`;
  static newBug = `${baseUrl}/issues/new?labels=bug&body=Describe%20your%20bug`;
  static newSuggestion = `${baseUrl}/issues/new?labels=enhancement&body=Describe%20your%20suggestion`;
  static contactMail = 'mailto:heritamus@fcsh.unl.pt';
}
